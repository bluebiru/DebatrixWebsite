{
  "version": 2,
  "installCommand": "echo 'No install command needed for static frontend'",
  "buildCommand": "echo 'No build command needed for static frontend'",
  "outputDirectory": "public",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/public/$1"
    }
  ]
}
